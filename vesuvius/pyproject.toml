[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "vesuvius"
dynamic = ["version"]
readme = "README.md"
requires-python = ">=3.14,<3.15"
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.14",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "Topic :: Software Development :: Libraries",
    "Topic :: Scientific/Engineering",
]
# Minimal core dependencies required by the Volume class.
dependencies = [
    "numpy>=2.0.2",
    "pillow>=11.3.0",
    "pynrrd>=1.1.3",
    "requests>=2.32.5",
    "pyyaml>=6.0.2",
    "fsspec>=2025.9.0",
    "numcodecs>=0.15.1,<0.17",
    "zarr>=2.18.7,<4",
    "s3fs>=2025.9.0",
    "cachetools>=6.2.4",
    "edt>=3.1.2",
]

[project.optional-dependencies]
# Minimal install explicitly named
volume-only = [
    "numpy>=2.0.2",
    "pillow>=11.3.0",
    "pynrrd>=1.1.3",
    "requests>=2.32.5",
    "pyyaml>=6.0.2",
    "fsspec>=2025.9.0",
    "numcodecs>=0.15.1,<0.17",
    "zarr>=2.18.7,<4",
    "s3fs>=2025.9.0",
]
# Machine-learning & training stack
models = [
    "torch>=2.12,<3",
    "accelerate>=1.13.0",
    "diffusers>=0.37.0",
    "pytorch-lightning>=2.5.5",
    "pytorch-optimizer>=3.8.0",
    "monai>=1.5.1",
    "huggingface-hub>=0.35.0",
    "timm>=1.0.20",
    "dynamic-network-architectures>=0.3.1",
    "nnunetv2",
    # Monorepo-only C++ package (resolved via [tool.uv.sources]); provides the vc /
    # vc3d bindings imported by vesuvius.neural_tracing. Not on PyPI, so it must stay
    # out of the base dependencies to keep `pip install vesuvius[volume-only]` working.
    "volume-cartographer",
    "numba>=0.60.0",
    "batchgenerators>=0.25.1",
    "batchgeneratorsv2",
    "einops>=0.8.1",
    "fft-conv-pytorch>=1.2.0",
    "fvcore>=0.1.5.post20221221",
    "scikit-image>=0.24.0",
    "scipy>=1.13.1",
    "connected-components-3d>=3.24.0",
    "pandas>=2.3.2",
    "tensorstore>=0.1.69",
    "tqdm>=4.67.1",
    "typed-argument-parser>=1.11.0",
    "wandb[media]>=0.22.0",
    "psutil>=7.1.0",
    "nest-asyncio>=1.6.0",

    # Data pipeline / I/O and compression
    "aiohttp>=3.12.15",
    "blosc2>=2.5.1",
    "dask>=2024.8.0",
    "dask-image>=2024.5.3",
    "lxml>=6.0.2",
    "opencv-python-headless>=4.11.0.86",
    "tifffile>=2024.8.30",
    "imagecodecs>=2026.3.6",
    # cuCIM ships manylinux wheels only; both import sites already fall back
    # gracefully when it is absent, so keep non-Linux platforms resolvable.
    "cucim-cu13>=26.6; sys_platform == 'linux'",
]
# Machine-learning & training stack
models-colab = [
    "torch>=2.12,<3",
    "accelerate>=1.13.0",
    "diffusers>=0.37.0",
    "pytorch-lightning>=2.5.5",
    "pytorch-optimizer>=3.8.0",
    "monai>=1.5.1",
    "huggingface-hub>=0.35.0",
    "timm>=1.0.20",
    "dynamic-network-architectures>=0.3.1",
    "nnunetv2",
    "numba>=0.60.0",
    "batchgenerators>=0.25.1",
    "batchgeneratorsv2",
    "einops>=0.8.1",
    "fft-conv-pytorch>=1.2.0",
    "fvcore>=0.1.5.post20221221",
    "scikit-image>=0.24.0",
    "scipy>=1.13.1",
    "connected-components-3d>=3.24.0",
    "pandas>=2.3.2",
    "tensorstore>=0.1.69",
    "tqdm>=4.67.1",
    "typed-argument-parser>=1.11.0",
    "wandb[media]>=0.22.0",
    "psutil>=7.1.0",
    "nest-asyncio>=1.6.0",

    # Data pipeline / I/O and compression
    "aiohttp>=3.12.15",
    "blosc2>=2.5.1",
    "dask>=2024.8.0",
    "dask-image>=2024.5.3",
    "lxml>=6.0.2",
    "opencv-python-headless>=4.11.0.86",
    "tifffile>=2024.8.30",
    "imagecodecs>=2026.3.6",
    # cuCIM ships manylinux wheels only; both import sites already fall back
    # gracefully when it is absent, so keep non-Linux platforms resolvable.
    "cucim-cu13>=26.6; sys_platform == 'linux'",
]
# Rendering and geometry utilities
render = [
    "trimesh>=4.8.2",
    "opencv-python-headless>=4.11.0.86",
    "opencv-contrib-python>=4.11.0.86",
    "libigl>=2.6.1",
    "alphashape>=1.3.1",
    "connected-components-3d>=3.24.0",
    "simpleitk>=2.5.2",
    "tifffile>=2024.8.30",
]
# Graphical user interface and visualisation
gui = [
    "napari>=0.5.6",
    "magicgui>=0.10.1",
    "magic-class>=0.7.17",
    "pyqt6",
]

# Notebooks
notebooks = [
    "jupyterlab>=4",
    "ipykernel>=6",
    "matplotlib",
    "nest-asyncio>=1.6.0",
]

# Blending and finalization (minimal dependencies for vesuvius.blend_logits and vesuvius.finalize_outputs)
blending = [
    "scipy>=1.13.1",       # For scipy.ndimage.gaussian_filter in blending.py
    "tqdm>=4.67.1",        # Progress bars
    "nest-asyncio>=1.6.0", # Required by vesuvius.utils.catalog (imported via __init__)
]

tests = ["pytest>=8.4.2"]

# Geometry-driven TIFXYZ label production (no torch/GUI dependency)
label-transfer = [
    "scipy>=1.13.1",
    "tifffile>=2024.8.30",
    "imagecodecs>=2024.6.1",
    # Required by vesuvius.utils.cli's package import boundary.
    "aiohttp>=3.12.15",
    "nest-asyncio>=1.6.0",
]

# Convenience alias to install everything
all = [
    # Machine-learning & training stack
    "torch>=2.12,<3",
    "accelerate>=1.13.0",
    "diffusers>=0.37.0",
    "pytorch-lightning>=2.5.5",
    "pytorch-optimizer>=3.8.0",
    "monai>=1.5.1",
    "huggingface-hub>=0.35.0",
    "timm>=1.0.20",
    "numba>=0.60.0",
    "dynamic-network-architectures>=0.3.1",
    "nnunetv2",
    "volume-cartographer",  # monorepo-only, see the note in [project.optional-dependencies.models]
    "batchgenerators>=0.25.1",
    "batchgeneratorsv2",
    "einops>=0.8.1",
    "fft-conv-pytorch>=1.2.0",
    "fvcore>=0.1.5.post20221221",
    "scikit-image>=0.24.0",
    "scipy>=1.13.1",
    "pandas>=2.3.2",
    "tensorstore>=0.1.69",
    "tqdm>=4.67.1",
    "typed-argument-parser>=1.11.0",
    "wandb[media]>=0.22.0",
    "psutil>=7.1.0",

    # Rendering and geometry utilities
    "trimesh>=4.8.2",
    "opencv-python-headless>=4.11.0.86",
    "opencv-contrib-python>=4.11.0.86",
    "libigl>=2.6.1",
    "alphashape>=1.3.1",
    "connected-components-3d>=3.24.0",
    "simpleitk>=2.5.2",
    "tifffile>=2024.8.30",
    "imagecodecs>=2026.3.6",

    # GUI and visualisation
    "napari>=0.5.6",
    "magicgui>=0.10.1",
    "magic-class>=0.7.17",
    "pyqt6",

    "nest-asyncio>=1.6.0",

    # Development / build tooling
    "build",
    "pybind11>=3.0.1",
    "setuptools>=80.9.0",
    "wheel",

    # Tests
    "pytest>=8.4.2",

    # Data pipeline / I/O and compression
    "aiohttp>=3.12.15",
    "blosc2>=2.5.1",
    "dask>=2024.8.0",
    "dask-image>=2024.5.3",
    "lxml>=6.0.2",
    "cucim-cu13>=26.6; sys_platform == 'linux'",
]

[project.urls]
Homepage = "https://github.com/ScrollPrize/villa"

[project.scripts]
"vesuvius.accept_terms" = "vesuvius.install.accept_terms:main"
"vesuvius.predict" = "vesuvius.models.run.inference:main"
"vesuvius.blend_logits" = "vesuvius.models.run.blending:main"
"vesuvius.blend_and_finalize" = "vesuvius.models.run.blending:blend_and_finalize_main"
"vesuvius.finalize_outputs" = "vesuvius.models.run.finalize_outputs:main"
"vesuvius.compute_st" = "vesuvius.structure_tensor.run_create_st:main"
"vesuvius.napari_trainer" = "vesuvius.napari_trainer.main_window:main"
"vesuvius.voxelize_obj" = "vesuvius.image_proc.run.voxelize_objs:main"
"vesuvius.refine_labels" = "vesuvius.image_proc.run.edt_frangi_label:main"
"vesuvius.train" = "vesuvius.models.training.train:main"
"vesuvius.zarr_tasks" = "vesuvius.image_proc.run.zarr_tasks.__main__:main"
"vesuvius.find_patches" = "vesuvius.models.preprocessing.patches.cli:main"
"vesuvius.surface_preflight" = "vesuvius.surface_preflight:main"

[tool.setuptools]
include-package-data = true

[tool.setuptools.package-dir]
"" = "src"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
vesuvius = [
    "install/configs/*.yaml",
    "ink_detection/configs/*.json",
    "tifxyz_label_transfer/*.cpp",
    "tifxyz_label_transfer/*.md",
    "tifxyz_label_transfer/docs/*.md",
]

[tool.setuptools.cmdclass]
install = "vesuvius.install.custom_install.CustomInstallCommand"

[tool.setuptools.dynamic]
version = {attr = "vesuvius._version.VERSION"}

[tool.uv]
required-environments = [
    "sys_platform == 'linux' and platform_machine == 'aarch64'",
]

[[tool.uv.index]]
name = "nvidia"
url = "https://pypi.nvidia.com"

[tool.uv.sources]
volume-cartographer = { path = "../volume-cartographer", editable = true }
cucim-cu13 = { index = "nvidia" }
batchgeneratorsv2 = { path = "../segmentation/models/batchgeneratorsv2" }
nnunetv2 = { path = "../segmentation/models/arch/nnunet" }
